export type ScrollProgressRef = {
  current: number;
};

export type PlanetPalette = {
  low: string;
  mid: string;
  high: string;
  accent: string;
  atmosphere: string;
};

export type MoonSurface = {
  albedoMap: string;
  heightMap: string;
};
